"use client"

import Loading from "@/app/loading"

// Preview of the route-level loading skeleton (app/loading.tsx) in
// isolation. Public on local dev only — see middleware.
export default function LoadingSkeletonPreview() {
  return <Loading />
}
